CRICKET UNIVERSE V1.3.1 AUTH/ROLE HOTFIX

Fixes:
- Supabase access tokens are automatically refreshed.
- HTTP 401 cloud calls retry once after refreshing the session.
- Administrator role is re-verified through the protected is_game_admin() RPC.
- Own user_roles row is also checked as a secondary role source.
- Signed-in users show 'Checking account role…' during startup instead of being
  incorrectly displayed as Spectator.
- A failed network/auth lookup shows 'Cloud role unavailable' rather than
  incorrectly demoting the UI to Spectator.
- Session/role refresh runs periodically and whenever the browser becomes visible.
- Existing V1.3 server-auto match engine and audio remain unchanged.

Upload to GitHub:
- app.js
- index.html
(styles.css is included but unchanged.)
No new SQL is required.
